---
name: build-godot-slice
description: Build one high-fidelity Godot vertical slice from an approved scenario contract.
agent: implementation-engineer
---

Do not start until the scenario contract and physics acceptance tests exist.

Implement one end-to-end slice:
briefing → access → inspection → setup → operation → incident/recovery → shutdown → debrief.

Use reusable modules, deterministic seeds, quality presets and debug overlays. Run headless checks and tests before reporting completion.
